apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "v5.7.fullname" . }}-test-connection"
  labels:
    {{- include "v5.7.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "v5.7.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
